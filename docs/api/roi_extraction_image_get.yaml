根据job_id获取提取成功的ROI
---
tags:
- extraction
parameters:
- name: job_id
  in: query
  required: true
  schema:
    type: string
    example: "qwert"
responses:
  200:
    content:
      image/png:
        schema:
          type: string
          format: binary
  404:
    description: "Job id not found"
  429:
    description: "Too Many Requests"