提取手掌图像的ROI
---
tags:
- extraction
requestBody:
  content:
    multipart/form-data:
      schema:
        type: object
        required:
        - palm_image
        properties:
          palm_image:
            type: string
            format: binary
responses:
  200:
    description: OK
    content:
      image/png:
        schema:
          type: string
          format: binary
